// The live browser game currently lives in the repo root `game.js`.
// This TypeScript entry keeps Bun's build pipeline pointed at the latest game
// until we finish migrating the runtime into the modular `src/` tree.
import '../game.js';
